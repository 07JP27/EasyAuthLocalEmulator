name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Set the repository variable RUN_TESTS=true to enable all test steps.
  RUN_TESTS: ${{ vars.RUN_TESTS || 'false' }}

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-latest
          - macos-14

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore EasyAuthLocalEmulator.sln

      - name: Build
        run: dotnet build EasyAuthLocalEmulator.sln --configuration Release --no-restore

      - name: Run unit tests
        if: env.RUN_TESTS == 'true'
        run: >-
          dotnet test
          tests/EasyAuthLocalEmulator.UnitTests/EasyAuthLocalEmulator.UnitTests.csproj
          --configuration Release
          --no-build
          --no-restore

      - name: Install Playwright browsers
        if: env.RUN_TESTS == 'true'
        shell: pwsh
        run: >-
          pwsh
          tests/EasyAuthLocalEmulator.BrowserTests/bin/Release/net10.0/playwright.ps1
          install chromium webkit

      - name: Run Chromium tests
        if: env.RUN_TESTS == 'true'
        shell: pwsh
        run: |
          $env:BROWSER = "chromium"
          dotnet test tests/EasyAuthLocalEmulator.BrowserTests/EasyAuthLocalEmulator.BrowserTests.csproj --configuration Release --no-build --no-restore

      - name: Run WebKit tests
        if: env.RUN_TESTS == 'true'
        shell: pwsh
        run: |
          $env:BROWSER = "webkit"
          dotnet test tests/EasyAuthLocalEmulator.BrowserTests/EasyAuthLocalEmulator.BrowserTests.csproj --configuration Release --no-build --no-restore
